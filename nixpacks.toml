[phases.setup]
nixPkgs = ["python311", "nodejs-18_x"]

[phases.install]
cmds = [
  "npm install",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sh -c 'uvicorn api.main:app --host 0.0.0.0 --port $PORT & npx serve -s dist -l $PORT'"
